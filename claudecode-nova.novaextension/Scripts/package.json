{
  "name": "claudecode-nova-scripts",
  "version": "0.26.0",
  "private": true,
  "type": "commonjs",
  "description": "Node subprocess scripts for the Claude Code Nova extension. ws-server.js is CommonJS (node built-ins only); chat-session.mjs / cli-session.mjs are ES modules (Agent SDK, ws, node-pty). postinstall prunes node_modules to the runtime footprint \u2014 see prune-deps.mjs.",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.0",
    "node-pty": "^1.1.0",
    "ws": "^8.18.0",
    "zod": "^4.0.0"
  },
  "scripts": {
    "postinstall": "node prune-deps.mjs",
    "prune": "node prune-deps.mjs"
  }
}
